import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';

var root = document.getElementById('root')

if(root){
    ReactDOM.render(
        // <div>Hey</div>,
    <App />,root);
}